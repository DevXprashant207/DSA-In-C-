#include<iostream>
using namespace std;
int main(){
     int a;
     if(3 + 2 % 5) cout<<"This works\n";// boolen ki wjah se koi non zero vale ke liye 1 and 1 menns true
     if(a=10) cout<<"even this works\n";
     if(-5) cout<<"suprisingly even this works";//if bollen 0 means false so no code run inside if
}