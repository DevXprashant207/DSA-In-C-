#include<iostream>
#include<cmath>
using namespace std;
int main(){
     int a, b;
     cout<<"Enter a : ";
     cin>>a;
     cout<<"Enter b : ";
     cin>>b;
     cout<<"minmum number is : "<<min(a,b); 
     cout<<"\nmaximum number is : "<<max(a,b);
     cout<<"\nSquare root of a is = "<<sqrt(a);
     cout<<"\nSquare root of b is = "<<sqrt(b);
}